# Inbox

Drop notes here. The agent will triage items into `TODO.md` (near-term) or `ROADMAP.md` (longer-term), then empty this file back to its template header.

the default gutter should be 24 not 32, put it in a var and make sure any instance of 32 is controlled from respective variables. do it cleanly , not patch ontop of patch. the baseline unit should be 8px - a;; other values are already multiples of 8, and the layout grid should be multiplse of the baseline unit for all elements,