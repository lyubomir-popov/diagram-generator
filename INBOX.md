
# Inbox

Drop notes here. The agent will triage items into `TODO.md`, then empty this file back to its template header.

We need drag and drop in the layers palette. for example, the multiple belonging (complex-routing-usecase) "dev team" is supposedto be a abs pos container going acros the first 2 columns, protruding on the left - as a separate layer showing the multiple belonging, the children currently in it should be in "implementation". drag and drop would allow fixing this

abs pos items currently cannot be resized to the left  - resizging to the vleft makes the right expand, which is wierd.