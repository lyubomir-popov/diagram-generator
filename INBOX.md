# Inbox

Drop notes here. The agent will triage items into `TODO.md` (near-term) or `ROADMAP.md` (longer-term), then empty this file back to its template header.

for cases like these, we need a new stacked icon+text component - aligne dto the grid ideally: ![
    this would solve the keyline break from the icon pushing text.
](image.png)