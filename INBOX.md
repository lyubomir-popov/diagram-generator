# Inbox

Drop notes here. The agent will triage items into `TODO.md` (near-term) or `ROADMAP.md` (longer-term), then empty this file back to its template header.

can weimplement draw.io-style arrows that bend at 90 degree angles- benchmark/leanr about how arrows behave t here before implementing. make a list of arrow features we'd want - in draw io you can add points, we generally want only 90 degree turns