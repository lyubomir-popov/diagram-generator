# Inbox

Drop notes here. The agent will triage items into `TODO.md` (near-term) or `ROADMAP.md` (longer-term), then empty this file back to its template header.



per-box weight (so some boxes can pull closer their elements in the force layout) would be good


explain - is our d3-inspired force layout similar ot the force-directed graphviz fdp/sfdp/elk force implementaitons? is it better or more simplistic?



as per the vision, we need tostart discussing a DSL - is there a clear FOSS one to adopt? mermaid, graphviz, or something else? ideally the diagrams should be portable, but also not limit us to architecutres that are not fit for our usecase; if we take an existing DSL we'd still need to extend it for brand/layout specific needs. lets discuss.

ELK contians a lot of layouts; we need to check which ones are worth porting.
which is the best foss sugyIama IMPLEMENTATION we ca nport over faithfully?